# Copy to .env and fill in. NEVER commit .env (it's gitignored).
AGENT_PRIVATE_KEY=0x...     # the authorized agent EOA (pays gas; must hold a little ETH on Arbitrum)
WALLET_ADDRESS=0x...        # the BVCC Agent Wallet on Arbitrum One
CHAIN_ID=42161             # Arbitrum One
# Optional: a private/faster RPC instead of the public default
# RPC_URL=https://arb-mainnet.g.alchemy.com/v2/KEY
